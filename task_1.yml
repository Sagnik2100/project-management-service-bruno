info:
  name: task_1
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}tasks?taskId=0&projectId=0&taskStatus=0&taskTypeId=0&taskPriority=0"
  headers:
    - name: Authorization
      value: "{{token}}"
  params:
    - name: taskId
      value: "0"
      type: query
    - name: projectId
      value: "0"
      type: query
    - name: taskStatus
      value: "0"
      type: query
    - name: taskTypeId
      value: "0"
      type: query
    - name: taskPriority
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
