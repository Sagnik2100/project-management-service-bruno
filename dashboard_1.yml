info:
  name: dashboard_1
  type: http
  seq: 23

http:
  method: GET
  url: "{{baseUrl}}developer/dashboard/count"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
