info:
  name: user-assignment
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}tasks/assignment-members"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
