info:
  name: team-lead-dashboard-weekly-graph
  type: http
  seq: 29

http:
  method: get
  url: http://localhost:3000/api/developer/dashboard/weekly-task-progress-graph
  headers:
    - name: Authorization
      value: Bearer YOUR_JWT_TOKEN
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
