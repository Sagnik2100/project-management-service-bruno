info:
  name: get-profile-details
  type: http
  seq: 26

http:
  method: GET
  url: users/profile-details
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
