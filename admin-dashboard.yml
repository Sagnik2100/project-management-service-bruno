info:
  name: admin-dashboard
  type: http
  seq: 34

http:
  method: get
  url: "{{baseUrl}}admin/dashboard/count"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
