info:
  name: task type
  type: http
  seq: 19

http:
  method: GET
  url: "{{baseUrl}}master/task-type"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
