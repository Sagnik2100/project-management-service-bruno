info:
  name: update-notification-seen
  type: http
  seq: 32

http:
  method: POST
  url: "{{baseUrl}}notifications/update-seen-status"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "{{token}}"
  body:
    type: json
    data: "{\r\n  \"NotificationID\": 1\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
