info:
  name: update-notification-seen
  type: http
  seq: 32

http:
  method: GET
  url: "{{baseUrl}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
