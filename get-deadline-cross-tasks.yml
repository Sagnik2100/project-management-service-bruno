info:
  name: get-deadline-cross-tasks
  type: http
  seq: 30

http:
  method: get
  url: "{{baseUrl}}developer/dashboard/deadline-crossed"
  headers:
    - name: Authorization
      value: "{{token}}"
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
