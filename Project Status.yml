info:
  name: Project Status
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}master/project-status"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
