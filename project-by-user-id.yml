info:
  name: project-by-user-id
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}projects/projects-by-user-id"
  headers:
    - name: Authorization
      value: "{{token}}"
  body:
    type: form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
