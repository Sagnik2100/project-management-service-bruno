info:
  name: project-type
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}master/project-type"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
