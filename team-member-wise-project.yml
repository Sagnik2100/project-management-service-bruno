info:
  name: team-member-wise-project
  type: http
  seq: 23

http:
  method: GET
  url: "{{baseUrl}}lead/dashboard/team-member-graph"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
