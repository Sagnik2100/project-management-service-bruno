info:
  name: user-wise-status-dashboard
  type: http
  seq: 24

http:
  method: get
  url: http://localhost:3000/api/developer/dashboard/developer-wise-team-progress-graph
  headers:
    - name: Authorization
      value: Bearer YOUR_JWT_TOKEN
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
