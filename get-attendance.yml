info:
  name: get-attendance
  type: http
  seq: 36

http:
  method: GET
  url: "{{baseUrl}}attendance?date=2026-03-22"
  headers:
    - name: Authorization
      value: "{{token}}"
  params:
    - name: date
      value: 2026-03-22
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
