info:
  name: admin-get-project-wise-task
  type: http
  seq: 35

http:
  method: get
  url: "{{baseUrl}}admin/dashboard/project-wise-progress-graph"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
