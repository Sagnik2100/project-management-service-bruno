info:
  name: designations
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}designations"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
