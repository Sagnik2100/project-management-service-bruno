info:
  name: priority
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}master/priority"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
