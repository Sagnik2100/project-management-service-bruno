info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: '{"username":"lead","password":"admin@123"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
