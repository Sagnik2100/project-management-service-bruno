info:
  name: task status
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseUrl}}master/task-status"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
