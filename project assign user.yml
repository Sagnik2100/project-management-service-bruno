info:
  name: project assign user
  type: http
  seq: 17

http:
  method: POST
  url: "{{baseUrl}}projects/assign-user"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "{{token}}"
  body:
    type: json
    data: |-
      {
                "userId": 1,
                "projectId": 1
           }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
