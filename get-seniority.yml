info:
  name: get-seniority
  type: http
  seq: 33

http:
  method: get
  url: "{{baseUrl}}master/seniority-levels"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
