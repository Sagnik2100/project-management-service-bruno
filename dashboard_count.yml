info:
  name: dashboard_count
  type: http
  seq: 23

http:
  method: GET
  url: "{{baseUrl}}lead/dashboard/count"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
