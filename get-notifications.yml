info:
  name: get-notifications
  type: http
  seq: 31

http:
  method: GET
  url: "{{baseUrl}}notifications"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
