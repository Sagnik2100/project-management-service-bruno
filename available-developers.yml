info:
  name: available-developers
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}projects/available-developers"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
